[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

extend-exclude = ["build", "dist", ".venv", "venv", ".ipynb_checkpoints"]

[tool.nbqa.config]

addopts = ["--stdin-filename", "."]
mutate = false

[tool.nbqa.ruff]
